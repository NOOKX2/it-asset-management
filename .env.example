# Database (PostgreSQL)
DATABASE_URL="postgresql://itam:itam_password@localhost:5432/itam"

# NextAuth — required in production (min 32 bytes)
# Generate: openssl rand -base64 32
# Docker Compose reads AUTH_SECRET from the host .env on the server.
AUTH_SECRET="change-me-in-production"
